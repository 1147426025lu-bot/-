# 本仓库只收录自行产出的源码、论文与结果；
# 赛题下发材料与第三方字体不在此公开转发。

# === 赛题下发的原始数据与题面 ===
数据/
problem.txt
problem_full.txt

# === 第三方商业字体（方正隶书，授权不允许再分发）===
LiSu.ttf

# === 打包产物（可由 build_submission.py 重建）===
提交包/

# === LaTeX 编译中间文件 ===
# 逐文件列出而非 *.aux 通配：AI工具使用详情.tex/.pdf 是入仓库的正文，
# 用「AI工具使用详情.*」会连它们的改动一起挡在 git status 之外。
main.aux
main.log
main.out
main.toc
main.xdv
main.fls
main.fdb_latexmk
AI工具使用详情.aux
AI工具使用详情.log
AI工具使用详情.xdv
AI工具使用详情.fls
AI工具使用详情.fdb_latexmk
*.synctex.gz

# === 个人杂项 ===
clean.bat
常用LaTex代码指令.txt

# === 智能体工作目录（不进提交包，也不进公开仓库）===
# build_submission.py 的 SUPPORT 白名单只收 sections/figures/code/results，
# 这四个目录本来就进不了支撑材料；列在这里是为了挡住 `git add .` 把它们一并
# 扫进公开仓库——_ext/ 是外部方案的原样副本（他人代码，不宜转发），
# _run_logs/ 与 _probe/ 是求解日志与探针输出，_legacy/ 是旧解备份。
# 只忽略、不删除：它们仍在本地，需要时可随时取用。
_ext/
_probe/
_run_logs/
_legacy/

# === Python 字节码缓存 ===
__pycache__/
*.py[cod]

# === 系统 / 编辑器 ===
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
